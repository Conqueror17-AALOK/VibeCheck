{
  "name": "@vibecheck/person-b",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Turns a user prompt/image into a structured checklist of expected elements via the Claude API, and diffs that checklist against person-a's extraction.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "ajv": "^8.17.1",
    "@vibecheck/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
