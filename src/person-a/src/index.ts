// TODO(person-a): drive Playwright to render a page and extract elements
// (bounding boxes, computed style, visibility) into the shape defined by
// @vibecheck/shared's VibeCheckData type / schema.json.
export {};
