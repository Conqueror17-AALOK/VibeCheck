{
  "name": "vibecheck-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Orchestrator repo for VibeCheck: extraction (person-a), checklist generation + diff (person-b), and web app (person-c) workspace packages.",
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspace=src/person-c",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "validate": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "next": "16.3.0"
  }
}
